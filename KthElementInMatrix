int kthSmallest(int mat[MAX][MAX], int n, int k)
{
    int ans;
  vector<int>v;
  for(int i=0;i<n;i++){
      for(int j=0;j<n;j++){
          v.push_back(mat[i][j]);
      }
  }
  sort(v.begin(),v.end());
  for(int i=0;i<n;i++){
      ans=v[k-1];
  }
  return ans;
  
}
